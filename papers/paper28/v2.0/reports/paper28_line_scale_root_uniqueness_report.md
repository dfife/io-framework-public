# Paper 28 - Primitive Line-Scale Root Uniqueness Audit

Date: 2026-03-31

## Executive result

- `derived`: premise 1 of the determinant-selected Gaussian theorem is not
  arbitrary. The primitive line spectral scale is forced to be

  \[
  s = \sqrt{r_s^2 \Delta_1^{coex}},
  \]

  not `r_s^2 Delta_1^coex`, by first-order line-transport dimensionality.

- `derived`: under a length rescaling `g -> L^2 g`, the coexact Hodge Laplacian
  scales as

  \[
  \Delta_1^{coex} \to L^{-2} \Delta_1^{coex}.
  \]

  Therefore `(Delta_1^coex)^q` scales as `L^(-2q)`. A primitive line generator
  must scale as inverse length, `L^(-1)`, so necessarily

  \[
  2q = 1 \qquad \Rightarrow \qquad q = \frac12.
  \]

- `derived`: the principal-symbol version says the same thing. The Laplacian
  symbol is `|xi|^2`; line transport is generated by the cotangent norm `|xi|`,
  hence by `sqrt(Delta)`.

## Theorem

Let `A_q = (Delta_1^coex)^q` on the canonical `S^2` coexact carrier.
Under metric length rescaling `g -> L^2 g`,

\[
\Delta_1^{coex} \to L^{-2} \Delta_1^{coex},
\qquad
A_q \to L^{-2q} A_q.
\]

If `A_q` is to generate primitive line transport parameterized by arclength,
it must have inverse-length dimension, hence must scale as `L^(-1)`.
Therefore

\[
2q=1,
\qquad
q=1/2.
\]

So among homogeneous functional-calculus choices of the coexact Hodge Laplacian,
the unique primitive line-scale generator is

\[
\sqrt{\Delta_1^{coex}}.
\]

## Principal-symbol version

For the Hodge Laplacian on coexact `1`-forms,

\[
\sigma_2(\Delta_1^{coex})(x,\xi)=|\xi|_g^2 I.
\]

A reparametrization-invariant line generator must depend on the cotangent norm
`|xi|_g`, not on `|xi|_g^2`. Therefore the unique isotropic first-order
spectral scale is

\[
\sigma_1(\sqrt{\Delta_1^{coex}})(x,\xi)=|\xi|_g I.
\]

This is the pseudodifferential meaning of "primitive line spectral scale".

## Alternative q-family check

Using the determinant/Gaussian route

\[
N_\ell \propto \lambda_\ell^{-q K_{gauge}/x},
\]

the pivot shell consequences are:

- `q=0.25`: generator dimension `L^(-0.5)`, pivot pure slope `-0.018100097534`, pivot total `n_s=0.981932845734`
- `q=0.5`: generator dimension `L^(-1.0)`, pivot pure slope `-0.036200195041`, pivot total `n_s=0.963832748117`
- `q=0.75`: generator dimension `L^(-1.5)`, pivot pure slope `-0.054300292658`, pivot total `n_s=0.94573265061`
- `q=1.0`: generator dimension `L^(-2.0)`, pivot pure slope `-0.072400390193`, pivot total `n_s=0.927632553104`

So:

- `q=1/4` is too weak,
- `q=1/2` is the surviving mild-tilt route,
- `q=3/4` and `q=1` are too red.

This is not a fit. It is exactly the dimensional consequence of using a
first-order line generator rather than a second-order area/time generator.

## Conclusion

Premise 1 is now materially sharpened:

- `derived`: if the Paper 28 shell dressing is generated by primitive line
  transport on the canonical coexact carrier, the spectral scale must be
  `sqrt(Delta_1^coex)`.
- `conditional`: what remains open is the physical membership claim that the
  actual A-vacuum canonical extension is governed by that primitive line
  transport.
