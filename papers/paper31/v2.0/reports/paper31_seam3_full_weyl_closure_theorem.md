# Paper 31 Seam 3: Full Weyl-Response Bridge Closure Theorem

## Discipline label

- label state: `CONDITIONAL_VERIFIED`
- registry check: Vectors 1-5 reviewed. No unresolved load-bearing circularity-vector hit remains on this surface after the present audit.
- chain verification: inline-verified through the cited Paper 31 chain to assumed-good Papers 1-31 nodes; no hidden admission remains load-bearing on the active Paper 32 chain.
- admitted inputs surfaced here: none.
- usage rule: this theorem may be cited as a premise because it is `CONDITIONAL_VERIFIED`.


## Question

Can the remaining open parts of the Weyl-response bridge be closed beyond the
already-derived scalar/Born weak-lensing sector?

The remaining items were:

1. vector/tensor lensing,
2. post-Born corrections,
3. full survey/tomographic likelihood packaging.

## Executive result

Yes, in the exact observable-class sense that matters for the bridge.

The strongest honest statement is:

- `derived / scoped`: all **linear** weak-lensing distortion fields generated
  from scalar, vector, or tensor metric perturbations belong to the same
  Weyl/curvature `alpha = 2` family.
- `derived / scoped`: post-Born corrections do **not** create a new observable
  class; they are higher-degree composites of the same Weyl family.
- `derived / scoped`: survey/tomographic likelihood packaging does **not**
  create a new observable class; it is a class-preserving binning / projection /
  covariance layer on top of the same shear/convergence and density fields.

Therefore:

\[
\boxed{
\text{the Weyl-response bridge is now closed at observable-class level for the}
\ \text{full current weak-lensing pipeline.}
}
\]

What remains outside this closure is only nuisance-value determination, not a
new class gap.

## 1. Existing scalar closure

Already established in Paper 31:

\[
\alpha_\delta = \frac32,
\qquad
\alpha_\Phi = 2,
\qquad
\frac{\Sigma_{\rm IO}}{\mu_{\rm IO}} = x^{-1/2}.
\]

And the scalar/Born lensing Hodge package closes as

\[
\varphi,\ \kappa,\ \gamma_E \in \alpha = 2,
\qquad
\gamma_B = 0
\]

for the linear scalar/Born sector.

So the only remaining question is whether vector/tensor, post-Born, or survey
packaging activate a new rung. The answer is no.

## 2. Vector/tensor linear lensing

### 2.1 Internal carrier structure

Paper 22 already closes the spatial bundle split on the closed `S^3` slice:

- scalar carrier,
- coexact vector carrier,
- TT tensor carrier.

Paper 23 already closes the existence of genuine physical tensor perturbation
equations on the closed FRW/OS interior, and the broader stack already states
that vector and tensor channels are genuine sectors, not gauge artifacts.

So the remaining vector/tensor lensing channels are not imported matter
observables. They are geometric metric perturbation sectors.

### 2.2 Exterior accepted lensing grammar

Standard weak-lensing theory with general metric perturbations says that
scalar, vector, and tensor perturbations all feed the lensing Jacobi map and
generate gradient/curl deflections and E/B shear through the geodesic equation
in the perturbed spacetime.

External support:

- [PTEP 2014 weak lensing from scalar, vector, and tensor perturbations](https://academic.oup.com/ptep/article-abstract/doi/10.1093/ptep/ptu044/1558773)
- [arXiv:1403.4947](https://arxiv.org/abs/1403.4947)

Under Premise 2, that operator grammar is admissible inside the hole.

### 2.3 Class assignment

The key point is that vector/tensor lensing is still generated by metric
curvature/connection data, not by the timelike dust-current continuity
scalarization that uniquely selects `alpha = 3/2`.

So the same Paper 31 `L1/L2` logic extends:

1. the source is geometric/Weyl-side, not dust-current transport;
2. the observer-side map is a linear screen/Jacobi/Hodge operation;
3. sky operators preserve class and add no new A-sensitive slot.

Hence the vector/tensor linear lensing fields remain on the same Weyl family:

\[
\boxed{
\alpha_{\rm lens,lin}^{\rm scalar}

=
\alpha_{\rm lens,lin}^{\rm vector}

=
\alpha_{\rm lens,lin}^{\rm tensor}

= 2.
}
\]

This includes the linear B/curl sector when vector/tensor perturbations are
present.

### Theorem 31.S3.FW1

Status: `derived / scoped`

All linear observer-side weak-lensing fields generated by scalar, vector, or
tensor metric perturbations belong to the Weyl/curvature `alpha = 2` family.

## 3. Post-Born corrections

Standard post-Born corrections arise from iterating the same geodesic-deviation
/ lens-lens coupling operator beyond first order.

External support:

- [arXiv:1605.05662](https://arxiv.org/abs/1605.05662)
- [arXiv:1612.07263](https://arxiv.org/abs/1612.07263)

These corrections generate:

- higher-order convergence corrections,
- rotation/curl,
- nonzero B modes,
- bispectra and higher cumulants.

But class-wise they are still built from repeated insertions of the same
Weyl-side deflection field. By Paper 16 degree bookkeeping:

- repeated products raise **degree**,
- they do **not** create a new `alpha` rung.

So post-Born corrections are not a new observable class. They are higher-degree
Weyl-family composites.

### Theorem 31.S3.FW2

Status: `derived / scoped`

Post-Born lensing corrections belong to the same Weyl-response family as the
linear lensing field. They raise perturbative degree and generate additional
E/B/curl and higher-point structure, but they do not introduce a new
observable-class rung beyond `alpha = 2`.

### Practical boundary

For CMB lensing, the accepted post-Born correction to the convergence power is
sub-percent:

- `\lesssim 0.2%` on accessible scales in [arXiv:1605.05662](https://arxiv.org/abs/1605.05662)

So even operationally, the remaining post-Born sector is not a bridge-breaking
effect for current precision. It is a same-family higher-order correction.

## 4. Survey / tomographic likelihood package

Current cosmic-shear and CMB-lensing survey analyses use:

- source redshift distributions,
- tomographic binning,
- shear maps on the sky,
- E/B decomposition,
- pseudo-`C_\ell` or correlation-function estimators,
- covariance matrices,
- Gaussian or near-Gaussian likelihoods,
- nuisance calibrations such as multiplicative shear bias and `\Delta z`.

External support:

- DES Y3 harmonic-space cosmic shear likelihood:
  [MNRAS 515, 1942 (2022)](https://academic.oup.com/mnras/article-abstract/515/2/1942/6625643)
- Euclid pseudo-`C_\ell` covariance / Gaussian-likelihood sufficiency:
  [A&A 660, A59 (2022)](https://www.aanda.org/articles/aa/full_html/2022/04/aa42908-21/aa42908-21.html)

Class-wise, these operations are:

1. linear redshift-weighted projections,
2. linear sky transforms,
3. quadratic covariances / likelihoods.

By the same Paper 21 + Paper 31 `L2` logic:

- linear binning/projection preserves field class,
- E/B decomposition preserves field class,
- covariance/likelihood raises degree, not `alpha`,
- nuisance calibration multiplies or shifts measured fields but does not create
  a new primitive geometric or dust-current observable class.

So the full tomographic likelihood package is class-preserving. It mixes and
weights the already-closed Weyl and density sectors; it does not generate a new
bridge theorem target.

### Theorem 31.S3.FW3

Status: `derived / scoped`

Tomographic source binning, E/B decomposition, pseudo-`C_\ell` or equivalent
two-point estimators, covariance construction, and Gaussian survey-likelihood
packaging preserve the underlying Weyl-response and density observable classes.
They do not create a new observable-class rung beyond the already-closed
`alpha_\Phi = 2` lensing family and `alpha_\delta = 3/2` density family.

## 5. Full closure statement

Combining FW1, FW2, and FW3:

### Theorem 31.S3.FW (Full Weyl-response bridge closure theorem)

Status: `derived / scoped`

Under the lab premises and the accepted exterior weak-lensing operator grammar:

1. all linear lensing fields from scalar/vector/tensor metric perturbations
   belong to the Weyl/curvature `alpha = 2` family,
2. post-Born corrections are higher-degree composites of that same family,
3. full survey/tomographic likelihood packaging preserves these classes.

Therefore the Weyl-response bridge

\[
\frac{\Sigma_{\rm IO}}{\mu_{\rm IO}} = x^{-1/2}
\]

is closed at observable-class level for the full current weak-lensing pipeline.

Numerically, with `x = 1.519`,

\[
\frac{\Sigma_{\rm IO}}{\mu_{\rm IO}} = 0.811374048924,
\qquad
\eta_{\rm IO} = 2x^{-1/2}-1 = 0.622748097849
\]

if `\mu_{\rm IO}=1`.

## 6. Exact boundary

This closes the seam in the correct sense:

- `derived / scoped`: no remaining lensing-sector observable-class gap
- `not claimed`: all nuisance values are internally derived
- `not claimed`: vector/tensor/post-Born amplitudes are fixed by the core IO
  constants alone
- `not claimed`: survey calibration or tomography parameters are parameter-free

So the remaining open work is parameter/value modelling, not a missing
Weyl-response class theorem.
