# Paper 31 Native Three-Sector Tau Theorem

Date: 2026-04-03

## Question

After the branch-choice problem is closed, what exactly is the remaining native
CMB obstruction on the structured IO branch? Is it still a two-sector mismatch
between low-`ell` EE and lensing, or is the native `tau` variable conflating
more than two physical objects?

## Executive result

The strongest honest result is:

- `derived / scoped`: on the current IO stack, the native scalar CMB problem is
  a **three-sector** problem, not a two-sector problem.
- `derived / scoped`: the relevant sectors are:
  1. high-`ell` primary anisotropy plus low-`ell` TT,
  2. low-`ell` EE rescattering,
  3. native Weyl/lensing.
- `derived / scoped`: these three sectors do **not** carry the same leading
  dependence on a single native `tau`.
- `verified / scoped`: the completed Paper 31 profiles already show the
  directional conflict predicted by this split:
  - low-`ell` EE pulls upward in `tau`,
  - the full native likelihood sits on the `tau` floor,
  - the dominant floor pressure comes from the lensing side and the
    high-`ell` `A_eff` ridge, not from low-`ell` EE.

Therefore the correct sharpened blocker is:

\[
\boxed{
\text{the native shared-}\tau\text{ model is mis-typed because it conflates}
\ \text{(i) primary }A_{\rm eff}\text{ attenuation,}
\ \text{(ii) low-}\ell EE\text{ rescattering, and}
\ \text{(iii) Weyl/lensing response.}
}
\]

## 1. Sector 1: primary high-`ell` + low-`ell` TT

`derived / scoped`

In the standard Boltzmann chain, once the primary recombination source is built,
late Thomson rescattering attenuates the already-produced primary field through
the post-recombination visibility factor. This is exactly the sector in which
high-`ell` TT/TE/EE constrain

\[
A_{\rm eff}=A_s e^{-2\tau}.
\]

This is already visible in the Paper 31 structured `tau`-slice audit, where
`A_eff` stays nearly fixed while `A_s` rises with `tau`.

So the high-`ell` primary sector is not a direct measurement of low-`ell`
rescattering strength. It is an attenuation ridge on the already-produced
primary field.

Support:

- [paper31_full_io_planck_tau_no_interior_theorem.md](/opt/cosmology-lab/results/paper31/paper31_full_io_planck_tau_no_interior_theorem.md)

## 2. Sector 2: low-`ell` EE rescattering

`derived / scoped`

From the actual IO CLASS source path in
[perturbations.c](/opt/cosmology-lab/src/class_public/source/perturbations.c),
the scalar polarization source is

\[
S_P(\tau,k)=\sqrt{6}\,g(\tau)\,P(\tau,k)
\]

up to the already-fixed structured IO factors.

At late reionization, the visibility packet `g(\tau)` is localized in a low-z
window and the induced E-mode field is linear in that windowed rescattering
amplitude. Therefore for small late optical depth,

\[
E_{\ell m}^{\rm reio} \propto \tau_E,
\qquad
C_\ell^{EE,\rm reio}\propto A_s\,\tau_E^2
\]

to leading order.

So low-`ell` EE is the exact opposite of the primary `A_{\rm eff}` ridge: it
is a positive rescattering signal, not a pure attenuation sector.

`verified / scoped`

At the exact derived source-side constants

\[
A_s^{\rm native}=2.0072459972737347\times 10^{-9},
\qquad
\tau_{\rm cov,IO}=K_{\rm gauge}/2=0.02743640887145733,
\]

the low-`ell` EE-only likelihood improves relative to the floor point
`tau = 0.0021`:

- floor:
  \[
  -2\log\mathcal L = 359.268824
  \]
- derived `\tau_{\rm cov,IO}` point:
  \[
  -2\log\mathcal L = 354.274453
  \]

an improvement of

\[
\Delta(-2\log\mathcal L) = -4.994371.
\]

This is a direct check using only framework-derived constants.

Support:

- [paper31_lowE_only_tau_scan_report.txt](/opt/cosmology-lab/results/paper31/paper31_lowE_only_tau_scan_report.txt)
- [paper31_lowE_tau_cov_point_report.txt](/opt/cosmology-lab/results/paper31/paper31_lowE_tau_cov_point_report.txt)

## 3. Sector 3: native lensing / Weyl response

`derived / scoped`

Native CMB lensing is already closed on a different operator class:

- Weyl family on the `\alpha_\Phi=2` rung,
- exact curved kernel
  \[
  M_{\rm IO}(k)=x^{-1/2}\left(\frac{k^2-3K}{k_p^2-3K}\right)^{-1/4}.
  \]

This sector is not generated by late Thomson rescattering. It is generated by
the scalar Weyl response along the line of sight.

So native lensing is not licensed to share the same `tau` variable as low-`ell`
EE rescattering.

Support:

- [paper31_cmb_weyl_exact_curved_kernel_theorem.md](/opt/cosmology-lab/results/paper31/paper31_cmb_weyl_exact_curved_kernel_theorem.md)
- [paper31_native_lensing_lowE_sector_theorem.md](/opt/cosmology-lab/results/paper31/paper31_native_lensing_lowE_sector_theorem.md)

## 4. Consequence: no single native `tau`

`derived / scoped`

The three leading sector responses are inequivalent:

\[
\text{primary high-}\ell: \quad C_\ell^{\rm prim} \sim A_s e^{-2\tau_{\rm prim}}
\]

\[
\text{low-}\ell EE: \quad C_\ell^{EE,\rm reio} \sim A_s \tau_E^2
\]

\[
\text{lensing}: \quad C_L^{\phi\phi} \sim \Sigma_{\rm IO}^2\,\mathcal P_{\rm m}
\]

with the exact Weyl kernel and no direct rescattering insertion.

Therefore a single native parameter `tau` cannot be a theorem-grade common
carrier of all three sectors. It forces one variable to play three different
operator roles:

1. attenuation of the primary field,
2. generation of the low-`ell` E-mode rescattering bump,
3. control of a Weyl-response observable that is not in the same class.

## 5. Exact current status of derived constants

`derived`

The only theorem-grade `tau` currently in the stack remains

\[
\tau_{\rm cov,IO}=K_{\rm gauge}/2,
\]

and by object class it belongs to the reduced source-covariance sector.

`not derived`

The current stack still does **not** prove:

- a post-bridge readout theorem turning `f_\Gamma^2` into a theorem-grade
  late optical `tau`,
- a theorem identifying the native high-`ell` attenuation variable with
  `\tau_{\rm cov,IO}`,
- a theorem identifying the lensing sector with any shared native `tau`.

So the safe top-of-stack statement is:

- `tau_{\rm cov,IO}` is the unique derived `tau` constant currently available,
- low-`ell` EE is structurally compatible with a non-floor derived `tau`,
- native lensing is not on that same class,
- the shared native `tau` model is therefore mis-typed.

## 6. Honest frontier

Later Paper 31 sharpening identifies the first of those debts more precisely:

- [paper31_late_visibility_identification_theorem.md](/opt/cosmology-lab/results/paper31/paper31_late_visibility_identification_theorem.md)

That note proves that the scalar high-`ell` attenuation sector is already part
of the late visibility / reionization class, not an independent GTTP-like
readout class.

The next theorem target is now exact:

\[
\boxed{
\text{derive the late reionization visibility history from framework constants}
\ \text{while keeping Weyl/lensing on its already-closed exact kernel,}
}
\]

or else prove that the high-`ell` primary attenuation variable has its own
independent native readout theorem.

Later Paper 31 closure resolves this fork asymmetrically:

- [paper31_reionization_inheritance_theorem.md](/opt/cosmology-lab/results/paper31/paper31_reionization_inheritance_theorem.md)
- [paper31_native_As_tau_pair_decomposition_theorem.md](/opt/cosmology-lab/results/paper31/paper31_native_As_tau_pair_decomposition_theorem.md)

The first note proves that late reionization is an inherited local
astrophysical-emissivity sector, not another core-constant readout theorem.
The second note proves that the shared native `(A_s, tau_reio)` target is
mis-typed: `A_s` is native source-side, whereas `tau_reio` is a compressed
observable moment of an inherited late visibility history.

Under the Paper 31 scan-discipline rule, no new free late-sector parameter is
to be scanned unless it is first derived from

\[
x,\ \gamma,\ K_{\rm gauge},\ \Delta
\]

or explicit algebraic combinations of them.
