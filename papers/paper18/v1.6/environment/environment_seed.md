# Randomness and Seeds

The Paper 18 v1.6 bundle uses deterministic algebra, deterministic numerical integration, and deterministic frozen external-data outputs.

No random seed is used by the default validation path.
