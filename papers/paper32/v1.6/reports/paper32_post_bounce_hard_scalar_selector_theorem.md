# Paper 32: Post-Bounce Hard Scalar Selector Theorem

## Discipline label

- label state: `CONDITIONAL_VERIFIED`
- registry check: Vectors 1-5 reviewed. No unresolved load-bearing circularity-vector hit remains on this surface after the present audit.
- chain verification: verified recursively through the cited Paper 32 post-bounce bridge and selector chain to assumed-good Papers 1-31 nodes; all remaining non-Premise inputs are the explicit selector-rule or bridge-family admissions surfaced here.
- admitted inputs surfaced here: `ADMITTED_SELECTOR_RULE(post-bounce observer-bridge / semantic-restoration selector principles)`.
- usage rule: this theorem may be cited as a premise because it is `CONDITIONAL_VERIFIED`.


Date: 2026-04-05

## Question

Is there a matching fully selected package on the semantic-restored
hard-restart side?

## Executive result

Yes, conditionally.

If one chooses:

1. semantic restoration of `x`,
   \[
   x_{\rm post}(R)=\frac{r_s}{R};
   \]
2. a comoving clock bridge,
   \[
   t_{\rm post}=\tau+\tau_*;
   \]
3. a chosen seed optical branch together with optical moment recurrence,
   selecting
   \[
   \beta=1,\qquad \lambda=1;
   \]

then the semantic-side scalar observer package is uniquely fixed within that
chosen seed family.

## Theorem statement

### Theorem 32.CF.66 — Post-Bounce Hard Scalar Selector Theorem

Status: `conditional`

Premises:

1. the semantic-x restoration theorem;
2. the semantic clock restoration theorem;
3. the optical moment selector theorem on a chosen seed optical branch.

Statement:

Under Premises 1-3, the semantic-side post-bounce scalar observer package is
uniquely selected within the chosen seed optical family by

\[
x_{\rm post}(R)=\frac{r_s}{R},
\qquad
\beta=1,
\qquad
\lambda=1,
\qquad
t_{\rm post}=\tau+\tau_*.
\]

It has the generic hard-restart morphology described by Theorem 32.CF.61,
unless the seed optical kernel is specially tuned to vanish at the clamp.

## Exact boundary

What this proves:

- on the semantic-restored side, the scalar observer package can also be fully
  selected within a chosen seed optical family.

What this does **not** prove:

- that semantic restoration is physically realized;
- that the chosen seed optical branch is uniquely supplied by the raw stack.

## Final verdict

The semantic-restored hard-restart package is as selectable as the continuous
soft-restart package once a seed optical branch is chosen.
