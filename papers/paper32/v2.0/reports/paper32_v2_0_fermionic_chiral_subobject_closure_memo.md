# Paper 32 v2.0 Fermionic/Chiral IO-EC Sub-Object Closure Memo

Date: 2026-05-21

Question: Can the four open Part VIII fermionic/chiral IO-EC sub-objects be
closed from the existing IO stack through Paper 35 plus standard physics,
without fitted parameters or hidden selector principles?

## Executive Verdict

The four objects do not all close. The strongest honest result is:

1. Horizon spinor/chiral boundary state: **partial closure**. A canonical
   quasi-free CAR KMS state is forced on a fixed lowest-shell fermionic mode
   once the fermionic/chiral carrier, dynamics, and chirality sector are fixed.
   The stack does not select a preferred chirality.
2. Radial spin transport `U_rad(R)`: **closed within the admitted extension**.
   It is the unique spin parallel transport generated by the induced spin
   connection along the chosen radial support curve. This is not a new fitted
   parameter.
3. Absolute EC source normalization: **partial, with a new blocker**. The
   support-conformal factor and Casimir form can be constrained, but the current
   artifact route that sets `p_H = 1/2` is not theorem-safe for a horizon KMS
   state. A true Schwarzschild horizon KMS mode gives `beta_H hbar c/r_s = 4pi`
   for the lowest `S^2` Dirac frequency, so the occupation is
   `1/(exp(4pi)+1)`, not `1/2`, unless an additional IO interior-readout
   state-selection theorem is supplied.
4. Minimal Holst vs conservative ECKS/Nieh-Yan selector: **partial/no-go**.
   The conservative ECKS/Nieh-Yan-completed class is theorem-safe. Minimal
   Holst with active `gamma_BI` remains unselected by the current stack.

Therefore Theorem 32.PC can tighten only to:

`DERIVED/CONDITIONAL_VERIFIED` within the explicitly admitted
fermionic/chiral IO-EC extension for carrier placement, radial spin transport,
and deep-bulk decoupling; the headline 222 Gyr cycle remains conditional on the
imported high-density EC/Poplawski bounce attachment and on unresolved
state/normalization/selector boundaries.

## External Standard Physics Inputs Used

- Standard spin geometry: a spin structure and metric-compatible spin
  connection determine spinor parallel transport by a first-order linear ODE.
- Standard black-hole QFT/KMS structure: stationary quasifree states on
  bifurcate Killing horizons are KMS at Hawking temperature under the Kay-Wald
  type hypotheses; fermionic finite-mode CAR KMS states are unique once the
  one-particle Hamiltonian and inverse temperature are fixed.
- Einstein-Cartan-Sciama-Kibble theory: torsion is algebraically constrained by
  spin density and produces a local four-fermion/axial spin-contact term, not a
  propagating torsion Green function.
- Holst/Nieh-Yan fermion coupling literature: minimally coupled Holst fermions
  and Nieh-Yan-completed/nonminimal fermion couplings are physically distinct
  until a parity/chiral selector is supplied; the Nieh-Yan-completed route
  removes `gamma_BI` from classical effective dynamics.

## 1. Horizon Spinor/Chiral Boundary State

Status: **partial**.

Proposed label:

`DERIVED/CONDITIONAL_VERIFIED` for the fixed-mode CAR KMS state; `DERIVED/NO-GO`
for chirality selection from the present stack.

### Theorem 32.FC1: Fixed-Mode Fermionic KMS Seed Theorem

Statement. On an admitted fermionic/chiral horizon bridge carrier, after
choosing:

1. the lowest `S^2` Dirac shell,
2. one chirality sector,
3. the boundary one-particle Hamiltonian,
4. the Hawking/KMS inverse temperature,

the boundary seed state on that finite CAR mode algebra is unique.

Proof. A finite CAR algebra with a fixed one-particle Hamiltonian has a unique
quasi-free Gibbs/KMS state at fixed inverse temperature. The state is determined
by the Fermi-Dirac covariance

```text
C = (1 + exp(beta h))^(-1).
```

For one fixed mode this is a single scalar occupation multiplying the identity
on the selected irreducible mode. The Hawking/KMS input fixes `beta`; the
one-particle Hamiltonian fixes `h`; hence the covariance is unique.

Chain:

```text
Theorem 32.FC1
<- admitted Paper 32 fermionic/chiral bridge carrier
<- standard finite CAR quasi-free KMS uniqueness
<- standard black-hole horizon KMS/Hawking state theorem under stationary
   horizon hypotheses
<- P1 (Schwarzschild horizon boundary for the IO universe)
AND P2 (standard exterior quantum field theory applies)
```

Boundary. This theorem does not select the chirality sector. It only selects
the state after chirality and mode typing are fixed.

### Theorem 32.FC2: Chirality-Selection No-Go

Statement. P1 + P2 + the current IO typed architecture do not select a preferred
horizon chirality for the new fermionic/chiral bridge sector.

Proof. The Schwarzschild/Oppenheimer-Snyder geometry is spherically symmetric
and supplies no parity-odd scalar or pseudoscalar horizon datum. The already
banked IO bosonic bridge classes are parity-even geometric/coexact classes and
do not contain a chiral asymmetry selector. P2 imports Standard Model chiral
interactions, but it does not by itself specify a horizon boundary population
imbalance or a parity-odd coupling to the support branch. Choosing one chirality
therefore requires an additional chiral boundary condition, parity-odd
invariant, or external matter-asymmetry input. Such a choice would be a new
premise if used to force the bridge state.

Chain:

```text
Theorem 32.FC2
<- P1 Schwarzschild/OS spherical symmetry and no parity-odd horizon datum
<- Paper 32 typed block separation and bosonic bridge exhaustion
<- P2 imports Standard Model exterior physics but not a horizon chirality
   boundary condition
```

Required addition to close. A theorem deriving a parity-odd horizon/chiral
selector from P1+P2, or an openly imported frozen external chiral boundary
condition.

## 2. Radial Spin Transport `U_rad(R)`

Status: **closed within the admitted fermionic/chiral extension**.

Proposed label:

`DERIVED/CONDITIONAL_VERIFIED`.

### Theorem 32.FC3: Radial Spin-Connection Transport Theorem

Statement. On the admitted fermionic/chiral bridge carrier over the IO
closed-support branch, `U_rad(R2,R1)` is uniquely the spin parallel transport

```text
U_rad(R2,R1) = P exp[- integral_{R1}^{R2} Gamma_spin],
```

where `Gamma_spin` is the metric-compatible spin connection induced by the IO
support geometry and chosen spin structure. It is unitary and preserves the
`SO(3)` total-angular-momentum representation type along radial support curves.

Proof. A spin structure on the support geometry induces a spinor bundle and a
metric-compatible spin connection. Along a fixed radial support curve, the
parallel-transport equation

```text
nabla_dotR psi = 0
```

is a first-order linear ODE. Existence and uniqueness give a unique transport
operator for each initial spinor. Metric compatibility preserves the spinor
inner product, so the transport is unitary. Spherical symmetry implies the
radial spin connection commutes with the `SO(3)` action; by representation
theory it cannot mix inequivalent angular-momentum irreducible sectors.

Chain:

```text
Theorem 32.FC3
<- admitted Paper 32 fermionic/chiral bridge carrier and spin structure
<- P1 closed K=+1 OS/Schwarzschild support geometry with radial support curves
<- standard spin geometry: spin connection and spin parallel transport
<- standard ODE uniqueness
<- standard SO(3) representation preservation under equivariant transport
```

Boundary. This closes existence, uniqueness, unitarity, and representation
stability of `U_rad`. It does not fix the boundary state, chirality, or absolute
Weyssenhoff source normalization.

## 3. Absolute EC Source Normalization

Status: **partial; current near-hit route blocked**.

Proposed label:

`DERIVED/CONDITIONAL_VERIFIED` for support-conformal descent and Casimir
structure inside the extension; `OPEN/PREMISE_GAP` for the final absolute
normalization anchoring to the Poplawski bounce density.

### Theorem 32.FC4: Support-Conformal Spinor Weight Theorem

Statement. If the admitted support seam acts locally, positively,
`SO(3)`-equivariantly, and only through the already-derived scalar clamp datum
`x_crit = Q^(-1/4)`, then its action on the horizon spinor bundle is conformal
rescaling by `x_crit`, and the spinor amplitude factor is

```text
x_crit^(-1/2) = Q^(1/8).
```

Proof. On `S^2`, a local positive `SO(3)`-equivariant scalar action has no
available tensorial freedom other than metric conformal rescaling. The only
available positive dimensionless clamp scalar is `x_crit`. For spinors on a
two-dimensional spin manifold, conformal rescaling `g -> Omega g` carries the
standard spinor conformal amplitude weight `Omega^(-1/2)`. Substituting
`Omega=x_crit=Q^(-1/4)` gives `Q^(1/8)`.

Chain:

```text
Theorem 32.FC4
<- admitted support-seam action on the fermionic/chiral extension
<- Paper 32 observer-domain boundary x_crit = Q^(-1/4)
<- Paper 15 Q = 1 + gamma_BI^2
<- standard conformal covariance of spinors on two-dimensional spin manifolds
<- P1 support geometry and S^2 horizon boundary
```

Boundary. The phrase "if the support seam acts..." is load-bearing. Without the
support-seam admissibility condition this is not a theorem of the old stack.

### Theorem 32.FC5: KMS-Casimir Normalization Boundary

Statement. On a fixed `J=1/2` chiral mode, the invariant quadratic source
observable is proportional to the Casimir `j(j+1)=3/4`, and its thermal
expectation is

```text
C_base = (3/4) p_H,
```

where `p_H` is the fermionic KMS occupation of that mode.

Proof. The selected `J=1/2` chiral quotient is irreducible under `SO(3)`.
Schur's lemma implies every positive invariant quadratic scalar is proportional
to the identity/Casimir. The occupation operator multiplies that scalar by its
KMS expectation. For `j=1/2`, `C_2=3/4`, hence `C_base=(3/4)p_H`.

Chain:

```text
Theorem 32.FC5
<- admitted lowest-shell one-chirality J=1/2 quotient
<- Schur's lemma / SO(3) representation theory
<- Theorem 32.FC1 fixed-mode CAR KMS state
```

Critical blocker. The prior extended-normalization artifact sets
`p_H = 1/2` by treating the dimensionless KMS energy as effectively zero. That
is not theorem-safe for a true Schwarzschild horizon KMS state. For the lowest
`S^2` Dirac frequency `omega=c/r_s` and Hawking inverse temperature
`beta_H = 4 pi r_s/(hbar c)`,

```text
beta_H hbar omega = 4 pi,
p_H = 1/(exp(4 pi)+1) = 3.4873301946946993e-6.
```

Then

```text
(3/4) p_H Q^(1/8) = 2.6334992786676017e-6,
```

not `(3/8)Q^(1/8) = 0.3775810049008211`.

Thus exact anchoring to the Poplawski bounce density is not closed by the
current state-selection chain. To recover `p_H=1/2`, Paper 32 would need a
separate theorem proving that the relevant boundary fermionic mode is governed
by an interior-readout or zero-energy centered KMS covariance rather than the
ordinary horizon Killing-energy KMS occupation. That theorem is not currently
banked.

Required addition to close. A theorem selecting the exact fermionic occupation
law used by the EC source lift and proving whether the correct `p_H` is the
horizon Killing occupation, the centered occupancy law, or another
bridge-readable quotient occupation. Without this, the absolute EC source
normalization remains open.

## 4. Minimal Holst vs Conservative ECKS/Nieh-Yan Selector

Status: **partial/no-go**.

Proposed label:

`DERIVED/CONDITIONAL_VERIFIED` for the conservative ECKS/Nieh-Yan class as the
theorem-safe route; `OPEN/PREMISE_GAP` or `DERIVED/NO-GO` for selecting minimal
Holst with active `gamma_BI` from the current stack.

### Theorem 32.FC6: Conservative EC Class Selector

Statement. Under P2, the theorem-safe high-density spin-torsion import for the
late-time support branch is the conservative Einstein-Cartan-Sciama-Kibble /
Nieh-Yan-completed effective class, not a unique minimal-Holst-with-active-
`gamma_BI` theory.

Proof. In conservative ECKS, torsion is algebraically constrained by spin
density and gives a local spin-contact correction; this is exactly the class
needed for the Paper 32 support-branch endpoint completion. Standard
Holst-fermion analyses show that minimal Holst couplings can introduce
Immirzi-sensitive parity/chiral structure, while Nieh-Yan-completed or
appropriate nonminimal formulations recover an Einstein-Cartan effective
dynamics where the Immirzi parameter is topological/classically non-dynamical.
The present IO stack contains no parity-odd or chiral selector that prefers the
minimal Holst branch over the conservative equivalence class. Therefore the
conservative ECKS/Nieh-Yan class is admissible and theorem-safe; minimal Holst
with active `gamma_BI` remains conditional.

Chain:

```text
Theorem 32.FC6
<- P2 imports standard exterior spin-torsion physics
<- standard ECKS algebraic torsion/spin-contact result
<- standard Holst/Nieh-Yan fermion-coupling distinction
<- Theorem 32.FC2 no current-stack chirality/parity selector
<- P1 supplies the IO support branch where the high-density completion attaches
```

Required addition to close minimal Holst. A theorem deriving a parity/chiral
selector that makes the active `gamma_BI` Holst coupling physically operative
in the high-density spin source, without fitting the bounce density.

## Effect On Theorem 32.PC And The 222 Gyr Cycle

Theorem 32.PC tightens in three ways:

- the new fermionic/chiral block is correctly typed as a separate deep-bulk
  support-sector extension;
- `U_rad(R)` is a standard, unique, unitary spin-connection transport;
- the conservative ECKS/Nieh-Yan class is the theorem-safe high-density class.

It does not fully promote the headline 222 Gyr cycle because:

- chirality is not selected from the current stack;
- the absolute EC source normalization is not fixed once the KMS occupation is
  audited against the true horizon-mode ratio;
- exact anchoring to the Poplawski bounce density still depends on an imported
  high-density criterion or a missing occupation/normalization theorem.

Final classification for the extension:

```text
Paper 32 Part VIII fermionic/chiral IO-EC extension:
DERIVED/CONDITIONAL_VERIFIED for carrier placement, U_rad, support-conformal
weight, and conservative ECKS/Nieh-Yan class boundaries.

OPEN/PREMISE_GAP for chirality selection and absolute EC source normalization.

DERIVED/NO-GO for claiming current-stack selection of minimal Holst with active
gamma_BI or claiming p_H=1/2 for a true horizon Killing-energy KMS mode without
an additional quotient-state theorem.
```
