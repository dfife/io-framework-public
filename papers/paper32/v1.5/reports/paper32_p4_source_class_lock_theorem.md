# Paper 32 - P4 Source-Class Lock Theorem

## Discipline label

- label state: `CLEAN`
- registry check: Vectors 1-5 reviewed. No unresolved load-bearing circularity-vector hit remains on this surface after the present audit.
- chain verification: verified through the cited Paper 32 chain to assumed-good Papers 1-31 nodes; no surfaced admission or unresolved local premise step remains load-bearing on this surface.
- admitted inputs surfaced here: none.
- usage rule: this theorem may be cited as a premise because it is `CLEAN`.


Date: 2026-04-05

## Question

After the full active-source derivation of P4, can the remaining obvious attack
surfaces be reduced further?

More precisely, can we eliminate:

1. **source-object ambiguity**:
   maybe the physical primordial readout is not really the active reduced
   source block;
2. **local payload ambiguity**:
   maybe the one-cell factor on the scalar source channel is not really
   exactly `Q = 1+\gamma^2`;
3. **DtN transport applicability ambiguity**:
   maybe the external order-one DtN transport theorem does not apply to the
   reduced source Hessian.

## Executive result

Two of the three attack surfaces can be eliminated theorem-grade.

### Eliminated

1. **Source-object ambiguity**

   The physical primordial `P4` object is uniquely locked to the active source
   block. There is no second live block in the present typed architecture that
   can carry the `P4` law.

2. **Local payload ambiguity**

   On the unique local scalar source channel, the shell-blind reduced
   Ashtekar-Barbero factor descends exactly to the scalar multiplier `Q`.

### Reduced but not eliminated

3. **DtN transport applicability ambiguity**

   This is no longer a hidden `P4` seam. It has been reduced to a direct
   challenge against the already-derived theorem that the active reduced source
   boundary action is DtN-type on the canonical coexact carrier.

So the final battlefield is no longer "P4 has many loose assumptions." It is:

\[
\boxed{
\text{either accept the derived DtN-typing theorem, or reject that theorem
itself.}
}
\]

## 1. Source-object lock

Paper 32's typed boundary-to-bulk projection theorem already proves:

\[
\mathcal T_{\rm IO}
=
\begin{pmatrix}
\mathcal P_{\rm src} & 0 & 0 \\
0 & U_{\rm therm} & 0 \\
C_{{\rm src}\to{\rm pert}} & C_{{\rm therm}\to{\rm pert}} & U_{\rm pert}^{S^3}
\end{pmatrix},
\]

with Items 1-4 belonging exactly to the source/readout block

\[
\mathcal P_{\rm src}.
\]

At the same time:

- Paper 31 proves the characteristic Stage-2 field does **not** inherit the
  primitive DtN character;
- reionization belongs to a different local-history block;
- the `S^3` perturbation solver block is typed separately and does not reduce
  to the one-slot source character.

So the physical primordial source law is not floating in the unreduced
framework. It is already typed.

Paper 28 then sharpens this further with the coefficient mechanism uniqueness
theorem:

\[
\text{inside the current linear primordial stack, the only surviving mechanism
class is}
\]
\[
\boxed{
\text{kernel-level one-slot finite transfer character on the DtN line semigroup.}
}
\]

Therefore:

\[
\boxed{
\text{within the present theorem stack, the physical primordial P4-carrying
observable is uniquely the active source-class object.}
}
\]

There is no second surviving mechanism class left inside the live primordial
stack.

## Theorem 32.KB.5 - Source-Object Lock Theorem

Status: `derived / scoped`

Under the typed Paper 32 architecture and the Paper 28 coefficient-mechanism
uniqueness theorem, every physical primordial observable capable of carrying
the mild source exponent must belong to the active one-slot source/readout
class. No other live typed block supports that role.

### Proof

1. The typed boundary-to-bulk theorem assigns Items 1-4 exactly to the source
   block `\mathcal P_{\rm src}`.
2. Paper 31 excludes the corresponding primitive DtN-character grammar from the
   surviving Stage-2 support.
3. Reionization and perturbation evolution inhabit distinct typed blocks with
   distinct proven no-go boundaries.
4. Paper 28 proves that, inside the live primordial stack, only one mechanism
   class survives for the mild source exponent: the kernel-level one-slot
   finite transfer character on the DtN line semigroup.

Therefore any physical primordial carrier of `P4` must be the active source
object, not an alternative block.

QED.

## 2. Local payload lock

Paper 23 proves that the local scalar bridge is unique up to normalization and
that the relevant equivariant scalar channel is unique.

Paper 28's coexact bridge population audit sharpens this:

- the local scalar channel is fiberwise one-dimensional.

Paper 28's reduced source-side analysis gives the shell-blind reduced
Ashtekar-Barbero factor

\[
O_\gamma = Q\,O_0,
\qquad
Q=1+\gamma^2.
\]

On a one-dimensional channel, any positive shell-blind endomorphism is exactly
scalar multiplication by its single eigenvalue. Hence the local source-cell
payload is literally

\[
\boxed{
O_\gamma^{\rm cell}=Q\,O_0^{\rm cell}.
}
\]

There is no extra multiplicity, determinant power, or hidden matrix structure
left on that channel.

## Theorem 32.KB.6 - Local Payload Lock Theorem

Status: `derived / scoped`

On the unique local scalar source channel, the reduced Ashtekar-Barbero
relative kernel acts exactly as multiplication by

\[
Q=1+\gamma^2.
\]

### Proof

1. The local scalar source channel is unique by bridge uniqueness.
2. That channel is fiberwise one-dimensional by the coexact bridge population
   theorem.
3. The reduced Ashtekar-Barbero source factor is shell-blind and positive,
   with value `Q`.
4. Every positive endomorphism of a one-dimensional real or complex line is
   multiplication by a single positive scalar.

Therefore on that channel the local payload is exactly `Q`.

QED.

## 3. What still remains

The only nontrivial remaining challenge is now:

> does the external order-one DtN transport theorem apply to the actual active
> reduced source Hessian?

But this is no longer a hidden assumption inside the P4 chain. It has been
reduced to the single explicit theorem gate:

\[
\boxed{
\text{Paper 28's theorem that the reduced boundary action is DtN-type on the
canonical coexact carrier.}
}
\]

If that theorem is accepted, the direct class assault and the full active-source
derivation go through.

If someone rejects it, they are not "finding a hidden P4 assumption." They are
reopening the derived DtN-typing theorem itself.

## Exact boundary

### Derived / scoped

- the physical primordial P4-carrying observable is uniquely locked to the
  active source block;
- the local one-cell scalar payload on that channel is exactly `Q`;
- the only remaining serious challenge is a direct challenge to the already-
  derived DtN-typing theorem.

### Not claimed

- that no one can ever dispute the Paper 28 DtN-typing theorem;
- that the entire unreduced framework shares one universal P4 law.

## Final status

\[
\boxed{
\text{Yes. Two attack surfaces are eliminated completely, and the third is
reduced to a direct challenge against an already-derived source-typing theorem.}
}
\]
