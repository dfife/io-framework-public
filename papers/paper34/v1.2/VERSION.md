# Version

Bundle version:

```text
paper34-v1.2-repro
```

Supported paper version:

```text
Paper 34 v1.2
```

Creation date:

```text
May 2026
```

Policy:

- This folder is immutable once published except for correction commits that
  are explicitly documented.
- Paper 34 v1.1 remains available at `papers/paper34/v1.1/`.
- A future Paper 34 v1.3 support package must be created as
  `papers/paper34/v1.3/`.
- Do not overwrite v1.1 outputs with v1.2 results, and do not overwrite v1.2
  outputs with later versions.

v1.2 update:

- Records the Paper 17 v1.5 FIRAS-fixed `R4_FIRAS` value for provenance.
- Verifies Paper 34's `H_ext(alpha,n)` scorecard has no active R4 dependency.
- Clarifies that the Planck CMB row is an H0 method-class row, not an
  independent CMB-temperature prediction.
- Leaves all six Paper 34 H0 predictions and residuals numerically unchanged.
