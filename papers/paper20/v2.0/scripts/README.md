# Scripts

All scripts are documented, standard-library-only readers of the frozen JSON
outputs in `../results/`. They are designed for reviewers who want to inspect
the arithmetic and claim boundaries without access to the private research lab.

Run all checks with:

```bash
python3 scripts/08_validate_expected_outputs.py
```
