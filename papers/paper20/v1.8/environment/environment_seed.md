# Random Seeds

The Paper 20 v1.8 bundle validator is deterministic and uses no random seeds.

The frozen outputs were generated from deterministic numerical integrations and
fixed external data products. No Monte Carlo sampling is part of the public
quickstart path.
