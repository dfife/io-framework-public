# External Dependencies

No external Python packages are required. The scripts use only the Python
standard library.

External observational or cross-paper inputs are cited and frozen in
`data/imported_constants.json`; this bundle does not redistribute external
observational datasets.

