# Environment Notes

This directory holds environment metadata needed to rerun public bundles.

For the initial Paper 31 bundle, the important environment facts are:

- local patched CLASS fork with IO-specific parameters
- PlanckLite-style likelihood data available in the private lab environment
- Python runtime and exact script inputs documented in the reports and manifests

If a future public release needs a fuller environment lock, add explicit package
versions and build notes here.

